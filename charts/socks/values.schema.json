{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "properties": {
    "allowed_dest_fqdn": {
      "default": "",
      "description": "Allowed destination address with regular expression pattern (Default allows all)",
      "required": [],
      "title": "Allowed Destination"
    },
    "allowed_ips": {
      "default": "",
      "description": "Allowed IP's that can connect to proxy separated with a comma",
      "required": [],
      "title": "Allowed IP's"
    },
    "enabled": {
      "default": false,
      "description": "Boolean to indicate that this chart is or will be installed in the cluster",
      "required": [],
      "title": "Enabled",
      "type": "boolean"
    },
    "global": {
      "description": "Global values are values that can be accessed from any chart or subchart by exactly the same name.",
      "required": [],
      "title": "global",
      "type": "object"
    },
    "kubee_internal": {
      "description": "@ignored Internal Kubee constant",
      "properties": {
        "install_cross_dependency": {
          "const": false,
          "default": "false",
          "description": " const: false @schema Constant used to not output template from kubee cross dependency in Chart.yaml Don't change, don't touch",
          "required": [],
          "title": "install_cross_dependency"
        }
      },
      "required": [],
      "title": "kubee_internal"
    },
    "namespace": {
      "default": "socks",
      "description": "The installation namespace",
      "minLength": 1,
      "required": [],
      "title": "Namespace"
    },
    "proxy_password": {
      "default": "",
      "description": "Set Proxy Password",
      "required": [],
      "title": "Proxy Password"
    },
    "proxy_port": {
      "default": 1080,
      "description": "Set the Proxy Port",
      "required": [],
      "title": "Proxy Port",
      "type": "integer"
    },
    "proxy_user": {
      "default": "",
      "description": "Set Proxy User",
      "required": [],
      "title": "Proxy User"
    },
    "replicas": {
      "default": 0,
      "description": "Number of replicas (Not up by default)",
      "required": [],
      "title": "The number of replicas",
      "type": "integer"
    },
    "require_auth": {
      "default": true,
      "description": "Is Authentication Required",
      "required": [],
      "title": "Authentication Required",
      "type": "boolean"
    }
  },
  "required": [
    "enabled",
    "namespace",
    "require_auth"
  ],
  "type": "object"
}
